---
const { title } = Astro.props;

const pageTitle = title ? `a:gender - ${title}` : "a:gender";
---

<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>{pageTitle}</title>
